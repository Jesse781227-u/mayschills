self.addEventListener('push', event => { const data = event.data ? event.data.json() : {}; event.waitUntil(self.registration.showNotification(data.title || "May's Chills", { body: data.body || 'Your order has an update.', icon: 'https://i.ibb.co/hJNHPS8T/IMG-20251112-WA0082.jpg', data: { reference: data.reference || '' } })); });
self.addEventListener('notificationclick', event => { event.notification.close(); event.waitUntil(clients.openWindow('/shop.html')); });
